import ReferralComponent from '../components/ReferralPage'

// This page wraps the component with page-level layout
export default function ReferralPage() {
  return (
    <div className="max-w-lg mx-auto">
      <ReferralComponent />
    </div>
  )
}