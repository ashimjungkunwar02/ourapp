<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!--
          FCM presentation defaults. These are read by firebase-messaging when a
          data/notification message arrives while the app is backgrounded or
          killed — i.e. exactly when the JS layer cannot style anything itself.
          Without the icon, Android renders a plain white square.

          Safe even when google-services.json is absent: the keys are simply
          unused because no FCM token is ever issued.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Android 13 (API 33) made notifications a RUNTIME permission. Without
      declaring this, FCM messages are delivered but silently discarded and
      PushNotifications.requestPermissions() resolves to 'denied' immediately.
      The JS layer asks for it via the plugin; the declaration is what lets the
      system show the dialog at all.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Rain/bonus celebration feedback and the hourly coin reminder. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
      Local coin-ready reminders are scheduled for a future time; WAKE_LOCK lets
      the alarm fire while the device is dozing (Doze/App Standby).
    -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
</manifest>
